# L is a list of integers ≥ 0.
#
# Prints a pattern of lines made of the character 'X'.
#
# For each n, a line of rank n + 1 consists of a single
# 'X' preceded by n tab characters.
#
# The pattern associated with an empty list is empty.
# The pattern associated with a non-empty list [m] + L1
# consists of:
# - m lines of rank 1;
# - if m >= 2, then between any two consecutive such
#   lines, the pattern associated with L1 is printed,
#   with the rank of each of its lines increased by 1.
#
# In particular:
# - if m == 0, nothing is printed;
# - if m == 1, a single line is printed and nothing else.

def f3(L):
    pass
    # REPLACE THE PASS STATEMENT ABOVE WITH YOUR CODE

# POSSIBLY DEFINE OTHER FUNCTIONS
